# Supabase project: srivaruni-erp (ap-south-1)
NEXT_PUBLIC_SUPABASE_URL=https://pkubyiwednioztrrkssx.supabase.co

# Publishable key. Safe to expose: it is the key the browser uses, and
# every request it makes is still subject to RLS.
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_V8AiqVPPTU9XMfLqFHmLuQ_QRt54iER

# Only for `npm run db:types`. Never referenced at runtime.
SUPABASE_PROJECT_ID=pkubyiwednioztrrkssx

# NEVER add the service_role key here. It bypasses every RLS policy in
# the database, including the ones that hide cost from staff.
